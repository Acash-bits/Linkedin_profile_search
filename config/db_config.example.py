# config/db_config.py
# ─────────────────────────────────────────────────────────────────
# Copy this file to config/db_config.py and fill in your credentials
# config/db_config.py is listed in .gitignore — never commit it
# ─────────────────────────────────────────────────────────────────

USA_DB = {
    "host": "localhost",
    "user": "root",
    "password": "YOUR_PASSWORD_HERE",
    "database": "usa"
}

UK_DB = {
    "host": "localhost",
    "user": "root",
    "password": "YOUR_PASSWORD_HERE",
    "database": "UK"
}
